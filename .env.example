# The docker-compose.yaml file provides sensible defaults for local development.
# You only need to provide secrets or override the defaults for production here.

# --- Secrets (Required for local development) ---
DB_PASSWORD=your_super_secret_password
GROQ_API_KEY=your_groq_api_key

# --- Production Overrides (Optional) ---

# DB_HOST=your_production_db_host
# DB_USER=your_production_db_user
# DB_NAME=your_production_db_name

# NEXT_PUBLIC_API_URL=https://your_production_api_url