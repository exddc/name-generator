FROM node:20

ARG NEXT_PUBLIC_API_URL
ARG NEXT_PUBLIC_SUGGEST_ENDPOINT

ENV NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL
ENV NEXT_PUBLIC_SUGGEST_ENDPOINT=$NEXT_PUBLIC_SUGGEST_ENDPOINT

WORKDIR /app

COPY package.json ./

RUN npm install

COPY . .

RUN npm run build

CMD ["npm", "start"]